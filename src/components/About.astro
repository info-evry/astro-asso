---
import SectionHeader from '../../design/src/components/SectionHeader.astro';
---

<section id="a-propos" class="section about">
  <div class="container">
    <SectionHeader
      label="À propos"
      title="Qui sommes-nous ?"
      description="L'Association des Étudiants en Informatique de l'Université d'Évry rassemble et accompagne les étudiants tout au long de leur parcours depuis 1993."
    />

    <div class="about-grid">
      <article class="about-card glass-card">
        <div class="card-icon">
          <span class="sfs sfs-2xl" data-sfs="@sfs:person.2.fill@" aria-hidden="true"></span>
        </div>
        <h3 class="card-title">Communauté</h3>
        <p class="card-description">
          Un réseau d'étudiants qui partagent leurs connaissances
          et s'entraident pour progresser ensemble.
        </p>
      </article>

      <article class="about-card glass-card">
        <div class="card-icon">
          <span class="sfs sfs-2xl" data-sfs="@sfs:checkmark.circle.fill@" aria-hidden="true"></span>
        </div>
        <h3 class="card-title">Ressources</h3>
        <p class="card-description">
          Accès à des supports de cours, des annales, des tutoriels
          et tous les outils pour vos études.
        </p>
      </article>

      <article class="about-card glass-card">
        <div class="card-icon">
          <span class="sfs sfs-2xl" data-sfs="@sfs:calendar.badge.clock@" aria-hidden="true"></span>
        </div>
        <h3 class="card-title">Événements</h3>
        <p class="card-description">
          Des hackathons, des conférences, des ateliers et des rencontres
          pour développer vos compétences.
        </p>
      </article>
    </div>
  </div>
</section>

<style>
  .about {
    position: relative;
  }

  .about::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 800px;
    height: 800px;
    background: radial-gradient(circle, rgba(0, 27, 165, 0.08) 0%, transparent 70%);
    pointer-events: none;
  }

  .about-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
  }

  .about-card {
    padding: var(--space-8);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .card-icon {
    width: 56px;
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(0, 27, 165, 0.1);
    border-radius: var(--radius-md);
    color: var(--color-primary);
  }

  .card-title {
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--color-text);
  }

  .card-description {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
  }

  @media (max-width: 900px) {
    .about-grid {
      grid-template-columns: 1fr;
      gap: var(--space-4);
    }

    .about-card {
      padding: var(--space-6);
    }
  }
</style>
